<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">

    <!-- To enable JMX Management -->
    <jmxConfigurator/>

    <appender name="file" class="ch.qos.logback.core.FileAppender">
        <file>${VOTE_SYS}/log/voting.log</file>

        <encoder>
            <charset>UTF-8</charset>
            <pattern>%-61(%d{HH:mm:ss.SSS} %-5level [%27.27t] %-55.55logger{0}) : %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%-61(%d{HH:mm:ss.SSS} %highlight(%-5level) %yellow([%27.27t]) %cyan(%-55.55logger{0})) : %green(%msg%n)</pattern>
        </encoder>
    </appender>

    <logger name="org.restaurant.voting" level="debug"/>
    <logger name="org.springframework.web.servlet" level="debug"/>
    <logger name="org.springframework.data.jpa" level="debug"/>
    <logger name="org.springframework.security" level="debug"/>

    <root level="info">
        <appender-ref ref="file"/>
        <appender-ref ref="console"/>
    </root>
</configuration>